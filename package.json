{
  "name": "transcript-portal",
  "version": "1.0.0",
  "description": "Simple portal to generate student transcripts (DOCX + PDF) without manual editing every time",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "docx": "^9.0.2",
    "pdfkit": "^0.15.0"
  }
}
